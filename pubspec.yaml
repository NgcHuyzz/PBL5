name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # 👇 เพิ่มตรงนี้
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.5

  google_fonts: ^6.1.0
  fl_chart: ^0.68.0  # สำหรับกราฟ
  intl: ^0.18.0  
  lottie: ^2.7.0
  flutter_animate: ^4.1.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
